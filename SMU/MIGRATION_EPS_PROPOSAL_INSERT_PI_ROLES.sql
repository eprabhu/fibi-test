DELIMITER $$
CREATE  PROCEDURE `MIGRATION_EPS_PROPOSAL_INSERT_PI_ROLES`(AV_ID INT)
BEGIN
DECLARE LI_SEQ_ERROR_LOG_ID INT;
DECLARE LS_ERROR_MSG 		VARCHAR(1000);
DECLARE LS_PERSON_ID 		VARCHAR(40);
DECLARE LI_PROPOSAL_ID		INT;
DECLARE LI_AWARD_PERSON_ROLE_ID	INT(12);
DECLARE LI_PROPOSAL_PERSON_ROLE_ID INT;
	DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
	BEGIN
		GET DIAGNOSTICS CONDITION 1 @sqlstate = RETURNED_SQLSTATE,
		 @errno = MYSQL_ERRNO, @msg = MESSAGE_TEXT;
		SET @full_error = CONCAT("ERROR ", @errno, " (", @sqlstate, "): ", @msg);
		SELECT @full_error INTO LS_ERROR_MSG;
		SELECT IFNULL(MAX(MIGRATION_ERROR_LOG_ID),0)+1 INTO LI_SEQ_ERROR_LOG_ID FROM MIGRATION_ERROR_LOG;
		INSERT INTO MIGRATION_ERROR_LOG(MIGRATION_ERROR_LOG_ID, LEGACY_PROJECT_ID,ERROR_TYPE, FILE_NAME, ERROR_MESSAGE, VALIDATION_TYPE, UPDATE_TIMESTAMP, UPDATE_USER)
		VALUES(LI_SEQ_ERROR_LOG_ID,LS_PROJECT_ID,'EXCEPTION','PROPOSAL_ROLES',LS_ERROR_MSG,'CREATION',NOW(),'quickstart');
		COMMIT;
	END;
	BEGIN
		DECLARE DONE1 INT DEFAULT FALSE;
		DECLARE CUR_SEL_DATA CURSOR FOR
		SELECT
		PERSON_ID,
		PROPOSAL_ID
		FROM EPS_PROPOSAL_PERSONS WHERE PROPOSAL_ID IN (SELECT PROPOSAL_ID FROM MIGRATION_PROPOSAL WHERE PROPOSAL_ID IS NOT NULL)
		AND PROP_PERSON_ROLE_ID = 3
		AND PERSON_ID IS NOT NULL;
		DECLARE CONTINUE HANDLER FOR NOT FOUND SET DONE1 = TRUE;
		OPEN CUR_SEL_DATA;
		INSERT_LOOP: LOOP
		FETCH CUR_SEL_DATA INTO
		LS_PERSON_ID,
		LI_PROPOSAL_ID;
		IF DONE1 THEN
			LEAVE INSERT_LOOP;
		END IF;
		SELECT IFNULL(MAX(PROPOSAL_PERSON_ROLE_ID),0)+1 INTO LI_PROPOSAL_PERSON_ROLE_ID FROM EPS_PROPOSAL_PERSON_ROLES;
		INSERT INTO EPS_PROPOSAL_PERSON_ROLES
		(PROPOSAL_PERSON_ROLE_ID,PROPOSAL_ID,PERSON_ID,ROLE_ID,UPDATE_TIMESTAMP,UPDATE_USER)
		VALUES(LI_PROPOSAL_PERSON_ROLE_ID,LI_PROPOSAL_ID,LS_PERSON_ID,5,NOW(),'quickstart');
		commit;
		END LOOP;
		CLOSE CUR_SEL_DATA;
		END;
END
$$
DELIMITER ;

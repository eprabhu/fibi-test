DELIMITER $$
CREATE  FUNCTION `FIBI_COI_DEV_PER_COI_DISC_FLAG`(
  AV_PROPOSAL_NUMBER VARCHAR(50),
  AV_PERSON_ID VARCHAR(50),
  AV_PROP_PERSON_ROLE_ID VARCHAR(50),
  AV_PERSON_PROJ_ROLE VARCHAR(50)
) RETURNS varchar(20) CHARSET utf8mb4
    DETERMINISTIC
BEGIN
  DECLARE LS_CERTIFICATION_FLAG VARCHAR(20);
  DECLARE LS_IS_PERSON_CERTIFIED CHAR(1);
  IF AV_PERSON_ID IS NULL THEN
    RETURN 'NOT_REQUIRED';
  END IF;
  RETURN 'REQUIRED';
  END
$$
DELIMITER ;

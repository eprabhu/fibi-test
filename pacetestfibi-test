[1mdiff --git a/.github/workflows/sync-core-to-coi.yml b/.github/workflows/sync-core-to-coi.yml[m
[1mindex 93bdb95..7cab0bb 100644[m
[1m--- a/.github/workflows/sync-core-to-coi.yml[m
[1m+++ b/.github/workflows/sync-core-to-coi.yml[m
[36m@@ -6,6 +6,9 @@[m [mon:[m
     paths:[m
       - 'Fibi-Vanilla/FIBI_CORE/**'[m
 [m
[32m+[m[32mpermissions:[m
[32m+[m[32m  contents: write[m
[32m+[m
 jobs:[m
   sync:[m
     runs-on: ubuntu-latest[m
[36m@@ -15,7 +18,19 @@[m [mjobs:[m
         with:[m
           fetch-depth: 2[m
       [m
[32m+[m[32m      - name: Check if CORE files changed[m
[32m+[m[32m        id: check-core[m
[32m+[m[32m        run: |[m
[32m+[m[32m          if git diff --name-only HEAD~1 HEAD | grep -q "Fibi-Vanilla/FIBI_CORE"; then[m
[32m+[m[32m            echo "changed=true" >> $GITHUB_OUTPUT[m
[32m+[m[32m            echo "CORE files changed"[m
[32m+[m[32m          else[m
[32m+[m[32m            echo "changed=false" >> $GITHUB_OUTPUT[m
[32m+[m[32m            echo "No CORE files changed"[m
[32m+[m[32m          fi[m
[32m+[m[41m      [m
       - name: Checkout COI[m
[32m+[m[32m        if: steps.check-core.outputs.changed == 'true'[m
         uses: actions/checkout@v4[m
         with:[m
           repository: eprabhu/coi[m
[36m@@ -23,15 +38,34 @@[m [mjobs:[m
           token: ${{ secrets.GITHUB_TOKEN }}[m
       [m
       - name: Sync CORE files[m
[32m+[m[32m        if: steps.check-core.outputs.changed == 'true'[m
         run: |[m
[31m-          # Copy CORE files[m
[31m-          cp -r Fibi-Vanilla/FIBI_CORE/* coi-repo/DB/CORE/[m
[32m+[m[32m          echo "Syncing CORE files..."[m
[32m+[m[41m          [m
[32m+[m[32m          # Create destination directory if it doesn't exist[m
[32m+[m[32m          mkdir -p coi-repo/DB/CORE[m
[32m+[m[41m          [m
[32m+[m[32m          # Copy CORE files (preserve directory structure)[m
[32m+[m[32m          cp -r Fibi-Vanilla/FIBI_CORE/* coi-repo/DB/CORE/ || true[m
           [m
           # Commit and push[m
           cd coi-repo[m
           git config user.name "github-actions[bot]"[m
           git config user.email "github-actions[bot]@users.noreply.github.com"[m
[31m-          git add DB/CORE/[m
[31m-          git diff --staged --quiet || git commit -m "Auto-sync CORE from fibi-test (commit: ${{ github.sha }})"[m
[31m-          git push origin main || git push origin master[m
[32m+[m[41m          [m
[32m+[m[32m          git add DB/CORE/ || true[m
[32m+[m[41m          [m
[32m+[m[32m          if [ -n "$(git status --porcelain DB/CORE)" ]; then[m
[32m+[m[32m            echo "Committing changes..."[m
[32m+[m[32m            git commit -m "Auto-sync CORE from fibi-test[m
[32m+[m
[32m+[m[32m          Source commit: ${{ github.sha }}[m
[32m+[m[32m          Source: ${{ github.repository }}"[m
[32m+[m[41m            [m
[32m+[m[32m            echo "Pushing to COI repository..."[m
[32m+[m[32m            git push origin main || git push origin master[m
[32m+[m[32m            echo "✅ Sync completed successfully!"[m
[32m+[m[32m          else[m
[32m+[m[32m            echo "No changes to commit. Already in sync."[m
[32m+[m[32m          fi[m
 [m

DELIMITER $$
CREATE DEFINER=`jhufibi`@`%` FUNCTION `FN_AREA_OF_RESEARCH_RULE`(
  AV_PROPOSAL_ID   int(10)
 ,RA_TYPE varchar(8)
) RETURNS varchar(6) CHARSET utf8mb4
    DETERMINISTIC
BEGIN
DECLARE LI_COUNT int;
DECLARE RETURN_STRING VARCHAR(5) DEFAULT 'FALSE';
 SELECT COUNT(*)
 INTO LI_COUNT
 FROM eps_proposal_resrch_areas p
 WHERE p.proposal_id=AV_PROPOSAL_ID
 AND   p.resrch_type_area_code = RA_TYPE;
IF LI_COUNT > 0 THEN
   SET RETURN_STRING = 'TRUE';
END IF;
RETURN RETURN_STRING;
END
$$
DELIMITER ;

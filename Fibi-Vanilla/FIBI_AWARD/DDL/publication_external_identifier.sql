SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS `publication_external_identifier`;

CREATE TABLE `publication_external_identifier` (
  `PUBLICATION_EXTERNAL_IDENTIFIER_ID` int(11) NOT NULL AUTO_INCREMENT,
  `RESEARCHER_PUBLICATION_ID` int(11) DEFAULT NULL,
  `IDENTIFIER_TYPE` varchar(100) DEFAULT NULL,
  `IDENTIFIER_VALUE` varchar(1000) DEFAULT NULL,
  `IDENTIFIER_URL` varchar(1000) DEFAULT NULL,
  `UPDATE_TIMESTAMP` datetime DEFAULT NULL,
  `UPDATE_USER` varchar(60) DEFAULT NULL,
  PRIMARY KEY (`PUBLICATION_EXTERNAL_IDENTIFIER_ID`),
  KEY `PUBLICATION_EXTERNAL_IDENTIFIER_FK1` (`RESEARCHER_PUBLICATION_ID`),
  CONSTRAINT `PUBLICATION_EXTERNAL_IDENTIFIER_FK1` FOREIGN KEY (`RESEARCHER_PUBLICATION_ID`) REFERENCES `researcher_publication` (`RESEARCHER_PUBLICATION_ID`)
) ;
SET FOREIGN_KEY_CHECKS = 1;
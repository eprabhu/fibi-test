

DROP TABLE IF EXISTS base_urls;
CREATE TABLE BASE_URLS (
	BASE_URL_ID INT auto_increment NOT NULL,
	LINK TINYTEXT NULL,
	UPDATE_TIMESTAMP DATETIME NULL,
	UPDATED_BY varchar(40) NULL,
	CONSTRAINT BASE_URL_ID PRIMARY KEY (BASE_URL_ID)
);
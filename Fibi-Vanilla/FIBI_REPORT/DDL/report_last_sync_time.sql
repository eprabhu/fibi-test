

DROP TABLE IF EXISTS `report_last_sync_time`;

CREATE TABLE `report_last_sync_time` (
  `REPORT_LAST_SYNC_TIME_ID` int PRIMARY KEY AUTO_INCREMENT,
  `LAST_SYNC_TIMESTAMP` datetime DEFAULT NULL,
  `CLAIM_LAST_SYNC_TIMESTAMP` datetime DEFAULT NULL,
  `REV_LAST_SYNC_TIMESTAMP` datetime DEFAULT NULL,
  `EXP_LAST_SYNC_TIMESTAMP` datetime DEFAULT NULL,
  `PR_LAST_SYNC_TIMESTAMP` datetime DEFAULT NULL,
  `DMP_LAST_SYNC_TIMESTAMP` datetime DEFAULT NULL
) ;
